pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                sh 'mvn clean package'
            }
        }

        stage('Verificar WAR') {
            steps {
                sh 'ls -la target'
            }
        }
    }
}