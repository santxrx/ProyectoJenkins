pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                bat 'mvn clean package'
            }
        }

        stage('Verificar WAR') {
            steps {
                bat 'dir target'
            }
        }
    }
}